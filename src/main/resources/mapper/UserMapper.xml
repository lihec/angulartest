<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spbt.dao.UserMapper">

    <select id="getUserList" resultType="com.spbt.domain.User">
        select usercode,password from user_test_table order by usercode
    </select>

    <select id="getUserPage" resultType="com.spbt.domain.User">
        select usercode,password from user_test_table order by usercode
    </select>

</mapper>